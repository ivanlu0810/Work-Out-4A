# 訓練計畫頁面重構指南

## 現況分析
- `training-plan.html` 有 11782 行
- 包含大量內嵌 CSS（約 3185 行）
- 包含大量內嵌 JavaScript（約 8500 行）
- 難以維護和優化

## 已完成的優化
1. ✅ 修復了資料庫排序問題
   - 修改 `dist/completion-api.php`，按照 `order_index` 排序
   - 修改 `exercise-completion-api.php`，確保查詢順序一致
   - 修改 `dist/training-plan.html`，加入排序邏輯

## 推薦的重構方案

由於檔案太大，建議採用以下方案：

### 方案 A：使用現代開發工具（推薦）
1. 使用 Vue.js 或 React 框架重構
2. 將功能拆分為多個組件
3. 利用 Webpack/Vite 等打包工具

### 方案 B：手動模組化
1. 創建 `training-plan-app.js` (頁面應用邏輯)
2. 創建 `training-plan-calendar.js` (行事曆功能)
3. 創建 `training-plan-stats.js` (統計功能)
4. 保持 CSS 和 JS 分離

### 方案 C：漸進式重構
1. 先分離 CSS（已完成腳本）
2. 再分離 JavaScript（按功能模組）
3. 逐步優化代碼

## 立即可行的優化

由於檔案太大，如果要立即改善載入速度，可以考慮：

1. **使用 Gzip 壓縮**
2. **啟用瀏覽器快取**
3. **延遲載入非關鍵資源**
4. **使用 CDN 載入外部資源**

## 當前狀態
- 資料庫排序問題已修復 ✓
- 功能正常運作 ✓
- 檔案結構需要進一步優化

## 下一步建議
1. 如果效能問題嚴重，考慮重構
2. 如果效能可接受，保持現狀並持續維護
3. 新增功能時採用分離的檔案結構

